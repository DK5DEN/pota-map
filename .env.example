# Required. The host this instance runs under: router rule, public tile URLs,
# and the name this installation gives when it calls other people's APIs.
PUBLIC_HOST=pota-map.example.org

# Optional: bridge to an issue tracker for the report panel.
# Without both values the panel stays hidden and the map works as usual.
BUG_ENDPOINT=
BUG_TOKEN=
